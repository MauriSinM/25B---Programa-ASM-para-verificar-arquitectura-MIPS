# 🖥️ Assembly x86_64 – Ejemplos y Prácticas

Este repositorio contiene programas escritos en **ensamblador**,
diseñados para validar el funcionamiento de nuestras arquitecturas MIPS DE VERILOG:

- Uso de registros de 64 bits (RAX, RBX, RCX, RDX…)
- Llamadas al sistema mediante `syscall`
- Estructura básica de un programa en ensamblador
- Ciclos utilizando la instrucción `loop`
- Instrucciones genéricas como `nop`, `inc`, `mov`, etc.
- Manejo de secciones `.data` y `.text`
- Creación de archivos ejecutables desde NASM

---

## 📁 Estructura del proyecto

--pendiente--
